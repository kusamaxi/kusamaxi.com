import { GetComponentAttributesOutput } from './get-component-attributes';
import Queue from '../core/queue';
interface GenerateComponentAttributesOptions {
    src: string;
    queue?: Queue;
    inputDir: string;
    outputDir: string;
    publicPath?: string;
    webp?: boolean;
    avif?: boolean;
    widths?: number[];
    quality?: number;
    skipGeneration?: boolean;
    skipPlaceholder?: boolean;
}
export default function generateComponentAttributes({ src, queue, inputDir, outputDir, publicPath, webp, avif, widths, quality, skipGeneration, skipPlaceholder, }: GenerateComponentAttributesOptions): Promise<GetComponentAttributesOutput>;
export {};
