(function () {
    'use strict';

    function noop() { }
    function run(fn) {
        return fn();
    }
    function blank_object() {
        return Object.create(null);
    }
    function run_all(fns) {
        fns.forEach(run);
    }
    function is_function(thing) {
        return typeof thing === 'function';
    }
    function safe_not_equal(a, b) {
        return a != a ? b == b : a !== b || ((a && typeof a === 'object') || typeof a === 'function');
    }
    let src_url_equal_anchor;
    function src_url_equal(element_src, url) {
        if (!src_url_equal_anchor) {
            src_url_equal_anchor = document.createElement('a');
        }
        src_url_equal_anchor.href = url;
        return element_src === src_url_equal_anchor.href;
    }
    function is_empty(obj) {
        return Object.keys(obj).length === 0;
    }
    function append(target, node) {
        target.appendChild(node);
    }
    function insert(target, node, anchor) {
        target.insertBefore(node, anchor || null);
    }
    function detach(node) {
        node.parentNode.removeChild(node);
    }
    function element(name) {
        return document.createElement(name);
    }
    function text(data) {
        return document.createTextNode(data);
    }
    function space() {
        return text(' ');
    }
    function listen(node, event, handler, options) {
        node.addEventListener(event, handler, options);
        return () => node.removeEventListener(event, handler, options);
    }
    function attr(node, attribute, value) {
        if (value == null)
            node.removeAttribute(attribute);
        else if (node.getAttribute(attribute) !== value)
            node.setAttribute(attribute, value);
    }
    function children(element) {
        return Array.from(element.childNodes);
    }
    function attribute_to_object(attributes) {
        const result = {};
        for (const attribute of attributes) {
            result[attribute.name] = attribute.value;
        }
        return result;
    }

    let current_component;
    function set_current_component(component) {
        current_component = component;
    }
    function get_current_component() {
        if (!current_component)
            throw new Error('Function called outside component initialization');
        return current_component;
    }
    function onMount(fn) {
        get_current_component().$$.on_mount.push(fn);
    }

    const dirty_components = [];
    const binding_callbacks = [];
    const render_callbacks = [];
    const flush_callbacks = [];
    const resolved_promise = Promise.resolve();
    let update_scheduled = false;
    function schedule_update() {
        if (!update_scheduled) {
            update_scheduled = true;
            resolved_promise.then(flush);
        }
    }
    function tick() {
        schedule_update();
        return resolved_promise;
    }
    function add_render_callback(fn) {
        render_callbacks.push(fn);
    }
    let flushing = false;
    const seen_callbacks = new Set();
    function flush() {
        if (flushing)
            return;
        flushing = true;
        do {
            // first, call beforeUpdate functions
            // and update components
            for (let i = 0; i < dirty_components.length; i += 1) {
                const component = dirty_components[i];
                set_current_component(component);
                update(component.$$);
            }
            set_current_component(null);
            dirty_components.length = 0;
            while (binding_callbacks.length)
                binding_callbacks.pop()();
            // then, once components are updated, call
            // afterUpdate functions. This may cause
            // subsequent updates...
            for (let i = 0; i < render_callbacks.length; i += 1) {
                const callback = render_callbacks[i];
                if (!seen_callbacks.has(callback)) {
                    // ...so guard against infinite loops
                    seen_callbacks.add(callback);
                    callback();
                }
            }
            render_callbacks.length = 0;
        } while (dirty_components.length);
        while (flush_callbacks.length) {
            flush_callbacks.pop()();
        }
        update_scheduled = false;
        flushing = false;
        seen_callbacks.clear();
    }
    function update($$) {
        if ($$.fragment !== null) {
            $$.update();
            run_all($$.before_update);
            const dirty = $$.dirty;
            $$.dirty = [-1];
            $$.fragment && $$.fragment.p($$.ctx, dirty);
            $$.after_update.forEach(add_render_callback);
        }
    }
    const outroing = new Set();
    function transition_in(block, local) {
        if (block && block.i) {
            outroing.delete(block);
            block.i(local);
        }
    }
    function mount_component(component, target, anchor, customElement) {
        const { fragment, on_mount, on_destroy, after_update } = component.$$;
        fragment && fragment.m(target, anchor);
        if (!customElement) {
            // onMount happens before the initial afterUpdate
            add_render_callback(() => {
                const new_on_destroy = on_mount.map(run).filter(is_function);
                if (on_destroy) {
                    on_destroy.push(...new_on_destroy);
                }
                else {
                    // Edge case - component was destroyed immediately,
                    // most likely as a result of a binding initialising
                    run_all(new_on_destroy);
                }
                component.$$.on_mount = [];
            });
        }
        after_update.forEach(add_render_callback);
    }
    function destroy_component(component, detaching) {
        const $$ = component.$$;
        if ($$.fragment !== null) {
            run_all($$.on_destroy);
            $$.fragment && $$.fragment.d(detaching);
            // TODO null out other refs, including component.$$ (but need to
            // preserve final state?)
            $$.on_destroy = $$.fragment = null;
            $$.ctx = [];
        }
    }
    function make_dirty(component, i) {
        if (component.$$.dirty[0] === -1) {
            dirty_components.push(component);
            schedule_update();
            component.$$.dirty.fill(0);
        }
        component.$$.dirty[(i / 31) | 0] |= (1 << (i % 31));
    }
    function init(component, options, instance, create_fragment, not_equal, props, append_styles, dirty = [-1]) {
        const parent_component = current_component;
        set_current_component(component);
        const $$ = component.$$ = {
            fragment: null,
            ctx: null,
            // state
            props,
            update: noop,
            not_equal,
            bound: blank_object(),
            // lifecycle
            on_mount: [],
            on_destroy: [],
            on_disconnect: [],
            before_update: [],
            after_update: [],
            context: new Map(parent_component ? parent_component.$$.context : options.context || []),
            // everything else
            callbacks: blank_object(),
            dirty,
            skip_bound: false,
            root: options.target || parent_component.$$.root
        };
        append_styles && append_styles($$.root);
        let ready = false;
        $$.ctx = instance
            ? instance(component, options.props || {}, (i, ret, ...rest) => {
                const value = rest.length ? rest[0] : ret;
                if ($$.ctx && not_equal($$.ctx[i], $$.ctx[i] = value)) {
                    if (!$$.skip_bound && $$.bound[i])
                        $$.bound[i](value);
                    if (ready)
                        make_dirty(component, i);
                }
                return ret;
            })
            : [];
        $$.update();
        ready = true;
        run_all($$.before_update);
        // `false` as a special case of no DOM component
        $$.fragment = create_fragment ? create_fragment($$.ctx) : false;
        if (options.target) {
            if (options.hydrate) {
                const nodes = children(options.target);
                // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
                $$.fragment && $$.fragment.l(nodes);
                nodes.forEach(detach);
            }
            else {
                // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
                $$.fragment && $$.fragment.c();
            }
            if (options.intro)
                transition_in(component.$$.fragment);
            mount_component(component, options.target, options.anchor, options.customElement);
            flush();
        }
        set_current_component(parent_component);
    }
    let SvelteElement;
    if (typeof HTMLElement === 'function') {
        SvelteElement = class extends HTMLElement {
            constructor() {
                super();
                this.attachShadow({ mode: 'open' });
            }
            connectedCallback() {
                const { on_mount } = this.$$;
                this.$$.on_disconnect = on_mount.map(run).filter(is_function);
                // @ts-ignore todo: improve typings
                for (const key in this.$$.slotted) {
                    // @ts-ignore todo: improve typings
                    this.appendChild(this.$$.slotted[key]);
                }
            }
            attributeChangedCallback(attr, _oldValue, newValue) {
                this[attr] = newValue;
            }
            disconnectedCallback() {
                run_all(this.$$.on_disconnect);
            }
            $destroy() {
                destroy_component(this, 1);
                this.$destroy = noop;
            }
            $on(type, callback) {
                // TODO should this delegate to addEventListener?
                const callbacks = (this.$$.callbacks[type] || (this.$$.callbacks[type] = []));
                callbacks.push(callback);
                return () => {
                    const index = callbacks.indexOf(callback);
                    if (index !== -1)
                        callbacks.splice(index, 1);
                };
            }
            $set($$props) {
                if (this.$$set && !is_empty($$props)) {
                    this.$$.skip_bound = true;
                    this.$$set($$props);
                    this.$$.skip_bound = false;
                }
            }
        };
    }

    /* src/Image.svelte generated by Svelte v3.42.4 */

    function create_if_block_2(ctx) {
    	let source;
    	let source_srcset_value;

    	return {
    		c() {
    			source = element("source");
    			attr(source, "type", "image/avif");

    			attr(source, "srcset", source_srcset_value = /*setSrcset*/ ctx[17]
    			? /*srcsetavif*/ ctx[4]
    			: undefined);

    			attr(source, "sizes", /*sizes*/ ctx[11]);
    		},
    		m(target, anchor) {
    			insert(target, source, anchor);
    		},
    		p(ctx, dirty) {
    			if (dirty & /*setSrcset, srcsetavif*/ 131088 && source_srcset_value !== (source_srcset_value = /*setSrcset*/ ctx[17]
    			? /*srcsetavif*/ ctx[4]
    			: undefined)) {
    				attr(source, "srcset", source_srcset_value);
    			}

    			if (dirty & /*sizes*/ 2048) {
    				attr(source, "sizes", /*sizes*/ ctx[11]);
    			}
    		},
    		d(detaching) {
    			if (detaching) detach(source);
    		}
    	};
    }

    // (124:4) {#if srcsetwebp}
    function create_if_block_1(ctx) {
    	let source;
    	let source_srcset_value;

    	return {
    		c() {
    			source = element("source");
    			attr(source, "type", "image/webp");

    			attr(source, "srcset", source_srcset_value = /*setSrcset*/ ctx[17]
    			? /*srcsetwebp*/ ctx[3]
    			: undefined);

    			attr(source, "sizes", /*sizes*/ ctx[11]);
    		},
    		m(target, anchor) {
    			insert(target, source, anchor);
    		},
    		p(ctx, dirty) {
    			if (dirty & /*setSrcset, srcsetwebp*/ 131080 && source_srcset_value !== (source_srcset_value = /*setSrcset*/ ctx[17]
    			? /*srcsetwebp*/ ctx[3]
    			: undefined)) {
    				attr(source, "srcset", source_srcset_value);
    			}

    			if (dirty & /*sizes*/ 2048) {
    				attr(source, "sizes", /*sizes*/ ctx[11]);
    			}
    		},
    		d(detaching) {
    			if (detaching) detach(source);
    		}
    	};
    }

    // (142:2) {#if !immediate && !hidePlaceholder}
    function create_if_block(ctx) {
    	let img;
    	let img_src_value;
    	let img_style_value;

    	return {
    		c() {
    			img = element("img");
    			attr(img, "class", "placeholder");
    			if (!src_url_equal(img.src, img_src_value = /*placeholder*/ ctx[5])) attr(img, "src", img_src_value);
    			attr(img, "alt", /*alt*/ ctx[0]);
    			attr(img, "width", /*imageWidth*/ ctx[12]);
    			attr(img, "height", /*imageHeight*/ ctx[18]);

    			attr(img, "style", img_style_value = /*useAspectRatioFallback*/ ctx[15]
    			? `width:${/*imageWidth*/ ctx[12]}px; height:${/*imageHeight*/ ctx[18]}px;`
    			: '');
    		},
    		m(target, anchor) {
    			insert(target, img, anchor);
    		},
    		p(ctx, dirty) {
    			if (dirty & /*placeholder*/ 32 && !src_url_equal(img.src, img_src_value = /*placeholder*/ ctx[5])) {
    				attr(img, "src", img_src_value);
    			}

    			if (dirty & /*alt*/ 1) {
    				attr(img, "alt", /*alt*/ ctx[0]);
    			}

    			if (dirty & /*imageWidth*/ 4096) {
    				attr(img, "width", /*imageWidth*/ ctx[12]);
    			}

    			if (dirty & /*imageHeight*/ 262144) {
    				attr(img, "height", /*imageHeight*/ ctx[18]);
    			}

    			if (dirty & /*useAspectRatioFallback, imageWidth, imageHeight*/ 299008 && img_style_value !== (img_style_value = /*useAspectRatioFallback*/ ctx[15]
    			? `width:${/*imageWidth*/ ctx[12]}px; height:${/*imageHeight*/ ctx[18]}px;`
    			: '')) {
    				attr(img, "style", img_style_value);
    			}
    		},
    		d(detaching) {
    			if (detaching) detach(img);
    		}
    	};
    }

    function create_fragment(ctx) {
    	let div;
    	let picture;
    	let t0;
    	let t1;
    	let img;
    	let img_srcset_value;
    	let img_alt_value;
    	let img_loading_value;
    	let img_class_value;
    	let t2;
    	let div_style_value;
    	let div_class_value;
    	let mounted;
    	let dispose;
    	let if_block0 = /*srcsetavif*/ ctx[4] && create_if_block_2(ctx);
    	let if_block1 = /*srcsetwebp*/ ctx[3] && create_if_block_1(ctx);
    	let if_block2 = !/*immediate*/ ctx[8] && !/*hidePlaceholder*/ ctx[14] && create_if_block(ctx);

    	return {
    		c() {
    			div = element("div");
    			picture = element("picture");
    			if (if_block0) if_block0.c();
    			t0 = space();
    			if (if_block1) if_block1.c();
    			t1 = space();
    			img = element("img");
    			t2 = space();
    			if (if_block2) if_block2.c();
    			this.c = noop;
    			attr(img, "srcset", img_srcset_value = /*setSrcset*/ ctx[17] ? /*srcset*/ ctx[2] : undefined);
    			attr(img, "sizes", /*sizes*/ ctx[11]);
    			attr(img, "alt", img_alt_value = /*loaded*/ ctx[16] ? /*alt*/ ctx[0] : undefined);
    			attr(img, "width", /*imageWidth*/ ctx[12]);
    			attr(img, "height", /*imageHeight*/ ctx[18]);
    			attr(img, "loading", img_loading_value = !/*immediate*/ ctx[8] ? 'lazy' : undefined);
    			attr(img, "class", img_class_value = "image " + (/*loaded*/ ctx[16] ? 'loaded' : ''));

    			attr(div, "style", div_style_value = "" + ((/*fixedWidth*/ ctx[10]
    			? `max-width:${/*width*/ ctx[6]}px;`
    			: '') + " --svimg-blur:" + /*blur*/ ctx[9] + "px; " + (/*aspectratio*/ ctx[7]
    			? `--svimg-aspect-ratio:${/*aspectratio*/ ctx[7]};`
    			: '')));

    			attr(div, "class", div_class_value = "wrapper " + /*className*/ ctx[1]);
    		},
    		m(target, anchor) {
    			insert(target, div, anchor);
    			append(div, picture);
    			if (if_block0) if_block0.m(picture, null);
    			append(picture, t0);
    			if (if_block1) if_block1.m(picture, null);
    			append(picture, t1);
    			append(picture, img);
    			append(div, t2);
    			if (if_block2) if_block2.m(div, null);
    			/*div_binding*/ ctx[28](div);

    			if (!mounted) {
    				dispose = listen(img, "load", /*onImgLoad*/ ctx[19]);
    				mounted = true;
    			}
    		},
    		p(ctx, [dirty]) {
    			if (/*srcsetavif*/ ctx[4]) {
    				if (if_block0) {
    					if_block0.p(ctx, dirty);
    				} else {
    					if_block0 = create_if_block_2(ctx);
    					if_block0.c();
    					if_block0.m(picture, t0);
    				}
    			} else if (if_block0) {
    				if_block0.d(1);
    				if_block0 = null;
    			}

    			if (/*srcsetwebp*/ ctx[3]) {
    				if (if_block1) {
    					if_block1.p(ctx, dirty);
    				} else {
    					if_block1 = create_if_block_1(ctx);
    					if_block1.c();
    					if_block1.m(picture, t1);
    				}
    			} else if (if_block1) {
    				if_block1.d(1);
    				if_block1 = null;
    			}

    			if (dirty & /*setSrcset, srcset*/ 131076 && img_srcset_value !== (img_srcset_value = /*setSrcset*/ ctx[17] ? /*srcset*/ ctx[2] : undefined)) {
    				attr(img, "srcset", img_srcset_value);
    			}

    			if (dirty & /*sizes*/ 2048) {
    				attr(img, "sizes", /*sizes*/ ctx[11]);
    			}

    			if (dirty & /*loaded, alt*/ 65537 && img_alt_value !== (img_alt_value = /*loaded*/ ctx[16] ? /*alt*/ ctx[0] : undefined)) {
    				attr(img, "alt", img_alt_value);
    			}

    			if (dirty & /*imageWidth*/ 4096) {
    				attr(img, "width", /*imageWidth*/ ctx[12]);
    			}

    			if (dirty & /*imageHeight*/ 262144) {
    				attr(img, "height", /*imageHeight*/ ctx[18]);
    			}

    			if (dirty & /*immediate*/ 256 && img_loading_value !== (img_loading_value = !/*immediate*/ ctx[8] ? 'lazy' : undefined)) {
    				attr(img, "loading", img_loading_value);
    			}

    			if (dirty & /*loaded*/ 65536 && img_class_value !== (img_class_value = "image " + (/*loaded*/ ctx[16] ? 'loaded' : ''))) {
    				attr(img, "class", img_class_value);
    			}

    			if (!/*immediate*/ ctx[8] && !/*hidePlaceholder*/ ctx[14]) {
    				if (if_block2) {
    					if_block2.p(ctx, dirty);
    				} else {
    					if_block2 = create_if_block(ctx);
    					if_block2.c();
    					if_block2.m(div, null);
    				}
    			} else if (if_block2) {
    				if_block2.d(1);
    				if_block2 = null;
    			}

    			if (dirty & /*fixedWidth, width, blur, aspectratio*/ 1728 && div_style_value !== (div_style_value = "" + ((/*fixedWidth*/ ctx[10]
    			? `max-width:${/*width*/ ctx[6]}px;`
    			: '') + " --svimg-blur:" + /*blur*/ ctx[9] + "px; " + (/*aspectratio*/ ctx[7]
    			? `--svimg-aspect-ratio:${/*aspectratio*/ ctx[7]};`
    			: '')))) {
    				attr(div, "style", div_style_value);
    			}

    			if (dirty & /*className*/ 2 && div_class_value !== (div_class_value = "wrapper " + /*className*/ ctx[1])) {
    				attr(div, "class", div_class_value);
    			}
    		},
    		i: noop,
    		o: noop,
    		d(detaching) {
    			if (detaching) detach(div);
    			if (if_block0) if_block0.d();
    			if (if_block1) if_block1.d();
    			if (if_block2) if_block2.d();
    			/*div_binding*/ ctx[28](null);
    			mounted = false;
    			dispose();
    		}
    	};
    }

    function instance($$self, $$props, $$invalidate) {
    	let fixedWidth;
    	let imageWidth;
    	let imageHeight;
    	let sizes;
    	let setSrcset;
    	let loaded;
    	let useAspectRatioFallback;
    	let { src } = $$props;
    	let { alt } = $$props;
    	let { class: className = '' } = $$props;
    	let { srcset } = $$props;
    	let { srcsetwebp = '' } = $$props;
    	let { srcsetavif = '' } = $$props;
    	let { placeholder = '' } = $$props;
    	let { width = '' } = $$props;
    	let { aspectratio } = $$props;
    	let { immediate = false } = $$props;
    	let { blur = 40 } = $$props;
    	let { quality = '' } = $$props;
    	let clientWidth;
    	let intersecting = false;
    	let native = false;
    	let container;
    	let imgLoaded = false;
    	let hasResizeObserver = true;
    	let hidePlaceholder = false;
    	let supportsCssAspectRatio = true;

    	function onImgLoad() {
    		$$invalidate(25, imgLoaded = true);

    		if (!immediate) {
    			setTimeout(
    				() => {
    					$$invalidate(14, hidePlaceholder = true);
    				},
    				250
    			); // sync with opacity transition duration
    		}
    	}

    	onMount(() => {
    		tick().then(() => {
    			let ro;

    			if (window.ResizeObserver) {
    				ro = new ResizeObserver(entries => {
    						$$invalidate(22, clientWidth = entries[0].contentRect.width);
    					});

    				ro.observe(container);
    			} else {
    				$$invalidate(26, hasResizeObserver = false);
    			}

    			$$invalidate(27, supportsCssAspectRatio = CSS.supports('aspect-ratio', 'var(--svimg-aspect-ratio)'));
    			$$invalidate(24, native = 'loading' in HTMLImageElement.prototype);

    			if (native || immediate) {
    				return () => {
    					if (ro) {
    						ro.unobserve(container);
    					}
    				};
    			}

    			const io = new IntersectionObserver(entries => {
    					$$invalidate(23, intersecting = entries[0].isIntersecting);

    					if (intersecting) {
    						io.unobserve(container);
    					}
    				},
    			{ rootMargin: `100px` });

    			io.observe(container);

    			return () => {
    				io.unobserve(container);

    				if (ro) {
    					ro.unobserve(container);
    				}
    			};
    		});
    	});

    	function div_binding($$value) {
    		binding_callbacks[$$value ? 'unshift' : 'push'](() => {
    			container = $$value;
    			$$invalidate(13, container);
    		});
    	}

    	$$self.$$set = $$props => {
    		if ('src' in $$props) $$invalidate(20, src = $$props.src);
    		if ('alt' in $$props) $$invalidate(0, alt = $$props.alt);
    		if ('class' in $$props) $$invalidate(1, className = $$props.class);
    		if ('srcset' in $$props) $$invalidate(2, srcset = $$props.srcset);
    		if ('srcsetwebp' in $$props) $$invalidate(3, srcsetwebp = $$props.srcsetwebp);
    		if ('srcsetavif' in $$props) $$invalidate(4, srcsetavif = $$props.srcsetavif);
    		if ('placeholder' in $$props) $$invalidate(5, placeholder = $$props.placeholder);
    		if ('width' in $$props) $$invalidate(6, width = $$props.width);
    		if ('aspectratio' in $$props) $$invalidate(7, aspectratio = $$props.aspectratio);
    		if ('immediate' in $$props) $$invalidate(8, immediate = $$props.immediate);
    		if ('blur' in $$props) $$invalidate(9, blur = $$props.blur);
    		if ('quality' in $$props) $$invalidate(21, quality = $$props.quality);
    	};

    	$$self.$$.update = () => {
    		if ($$self.$$.dirty & /*width*/ 64) {
    			$$invalidate(10, fixedWidth = !!(width && (/^[0-9]+$/).test(width)));
    		}

    		if ($$self.$$.dirty & /*fixedWidth, clientWidth, width*/ 4195392) {
    			$$invalidate(12, imageWidth = fixedWidth && clientWidth
    			? Math.min(clientWidth, width)
    			: fixedWidth ? width : clientWidth);
    		}

    		if ($$self.$$.dirty & /*imageWidth, aspectratio*/ 4224) {
    			$$invalidate(18, imageHeight = imageWidth / aspectratio);
    		}

    		if ($$self.$$.dirty & /*imageWidth*/ 4096) {
    			$$invalidate(11, sizes = imageWidth ? `${imageWidth}px` : undefined);
    		}

    		if ($$self.$$.dirty & /*intersecting, native, immediate, sizes, hasResizeObserver*/ 92276992) {
    			$$invalidate(17, setSrcset = (intersecting || native || immediate) && (sizes || !hasResizeObserver));
    		}

    		if ($$self.$$.dirty & /*imgLoaded, immediate*/ 33554688) {
    			$$invalidate(16, loaded = imgLoaded || immediate);
    		}

    		if ($$self.$$.dirty & /*supportsCssAspectRatio, aspectratio, fixedWidth, hasResizeObserver*/ 201327744) {
    			$$invalidate(15, useAspectRatioFallback = !supportsCssAspectRatio && aspectratio && (fixedWidth || hasResizeObserver));
    		}
    	};

    	return [
    		alt,
    		className,
    		srcset,
    		srcsetwebp,
    		srcsetavif,
    		placeholder,
    		width,
    		aspectratio,
    		immediate,
    		blur,
    		fixedWidth,
    		sizes,
    		imageWidth,
    		container,
    		hidePlaceholder,
    		useAspectRatioFallback,
    		loaded,
    		setSrcset,
    		imageHeight,
    		onImgLoad,
    		src,
    		quality,
    		clientWidth,
    		intersecting,
    		native,
    		imgLoaded,
    		hasResizeObserver,
    		supportsCssAspectRatio,
    		div_binding
    	];
    }

    class Image extends SvelteElement {
    	constructor(options) {
    		super();
    		this.shadowRoot.innerHTML = `<style>.wrapper{display:grid;grid:1fr / 1fr;gap:0px;grid-gap:0px;overflow:hidden}.wrapper>*{grid-area:1 / 1 / 2 / 2}.wrapper img{width:100%;height:auto;display:block;aspect-ratio:var(--svimg-aspect-ratio)}.image{opacity:0;transition:opacity 0.25s ease-in}.image.loaded{opacity:1}.placeholder{z-index:-1;filter:blur(var(--svimg-blur))}</style>`;

    		init(
    			this,
    			{
    				target: this.shadowRoot,
    				props: attribute_to_object(this.attributes),
    				customElement: true
    			},
    			instance,
    			create_fragment,
    			safe_not_equal,
    			{
    				src: 20,
    				alt: 0,
    				class: 1,
    				srcset: 2,
    				srcsetwebp: 3,
    				srcsetavif: 4,
    				placeholder: 5,
    				width: 6,
    				aspectratio: 7,
    				immediate: 8,
    				blur: 9,
    				quality: 21
    			},
    			null
    		);

    		if (options) {
    			if (options.target) {
    				insert(options.target, this, options.anchor);
    			}

    			if (options.props) {
    				this.$set(options.props);
    				flush();
    			}
    		}
    	}

    	static get observedAttributes() {
    		return [
    			"src",
    			"alt",
    			"class",
    			"srcset",
    			"srcsetwebp",
    			"srcsetavif",
    			"placeholder",
    			"width",
    			"aspectratio",
    			"immediate",
    			"blur",
    			"quality"
    		];
    	}

    	get src() {
    		return this.$$.ctx[20];
    	}

    	set src(src) {
    		this.$$set({ src });
    		flush();
    	}

    	get alt() {
    		return this.$$.ctx[0];
    	}

    	set alt(alt) {
    		this.$$set({ alt });
    		flush();
    	}

    	get class() {
    		return this.$$.ctx[1];
    	}

    	set class(className) {
    		this.$$set({ class: className });
    		flush();
    	}

    	get srcset() {
    		return this.$$.ctx[2];
    	}

    	set srcset(srcset) {
    		this.$$set({ srcset });
    		flush();
    	}

    	get srcsetwebp() {
    		return this.$$.ctx[3];
    	}

    	set srcsetwebp(srcsetwebp) {
    		this.$$set({ srcsetwebp });
    		flush();
    	}

    	get srcsetavif() {
    		return this.$$.ctx[4];
    	}

    	set srcsetavif(srcsetavif) {
    		this.$$set({ srcsetavif });
    		flush();
    	}

    	get placeholder() {
    		return this.$$.ctx[5];
    	}

    	set placeholder(placeholder) {
    		this.$$set({ placeholder });
    		flush();
    	}

    	get width() {
    		return this.$$.ctx[6];
    	}

    	set width(width) {
    		this.$$set({ width });
    		flush();
    	}

    	get aspectratio() {
    		return this.$$.ctx[7];
    	}

    	set aspectratio(aspectratio) {
    		this.$$set({ aspectratio });
    		flush();
    	}

    	get immediate() {
    		return this.$$.ctx[8];
    	}

    	set immediate(immediate) {
    		this.$$set({ immediate });
    		flush();
    	}

    	get blur() {
    		return this.$$.ctx[9];
    	}

    	set blur(blur) {
    		this.$$set({ blur });
    		flush();
    	}

    	get quality() {
    		return this.$$.ctx[21];
    	}

    	set quality(quality) {
    		this.$$set({ quality });
    		flush();
    	}
    }

    if (typeof window !== undefined && window.customElements) {
        customElements.define('s-image', Image);
    }

}());
